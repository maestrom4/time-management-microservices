apiVersion: v1
kind: Service
metadata:
  name: db-manager-service
  namespace: development
spec:
  selector:
    app: db-manager-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
